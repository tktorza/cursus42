<ul class="nav types-nav" role="tablist">
    {% if pdjs is not empty %}
    {% if iframe is defined and iframe and restaurant.primaryColor is not null %}
        <li><a href="#" class="category-filter anchor" data-target-category="pdj" style="color:{{ restaurant.primaryColor }}"> Plats du jour </a></li>
        {% else %}
        <li><a href="#" class="category-filter anchor" data-target-category="pdj"> Plats du jour </a></li>
        {% endif %}
    {% endif %}
    {% if meals is not empty %}
    {% if iframe is defined and iframe  and restaurant.primaryColor is not null %}
        <li><a href="#" class="category-filter anchor" data-target-category="meal" style="color:{{ restaurant.primaryColor }}"> Formules </a></li>
        {% else %}
    <li><a href="#" class="category-filter anchor" data-target-category="meal"> Formules </a></li>
        {% endif %}
    {% endif %}
    {% for type in types %}
        {% if type is not null %}
            {% if iframe is defined and iframe and restaurant.primaryColor is not null  %}
                <li><a href="#" class="category-filter anchor" data-target-category="{{ type }}"  style="color:{{ restaurant.primaryColor }}">{{ type }}</a></li>
            {% else %}
                <li><a href="#" class="category-filter anchor" data-target-category="{{ type }}">{{ type }}</a></li>
            {% endif %}
        {% endif %}
    {% endfor %}
    <li><a href="#" class="filter-none">Catalogue</a></li>
    {% if (iframe is defined and iframe) %}
    {% for discount in discounts %}
    {% if discount.isOnline == 1 and  discount.isMultisite == true %}
    {% if discount.length > 0 %}
        <li class="offers-wrapper">
        <span class="offers-header open">
          <span class="title-line">
              <span class="flex">OFFRES ({{ discount.length }})</span>
              <span class="fa fa-chevron-right"></span>
              <span class="fa fa-chevron-up"></span>
          </span>
          <div class="offers-bottom"></div>
        </span>
            <ul class="offers">
                {% if (iframe is defined and iframe) %}
                    {% for discount in discounts %}
                        {% if discount.isOnline == 1 and  discount.isMultisite == true %}
                            <li>{{ discount.description }}</li>
                        {% endif %}
                    {% endfor %}
                {% else %}
                    {% for discount in discounts %}
                        {% if discount.isOnline == 1 %}
                            <li>{{ discount.description }}</li>
                        {% endif %}
                    {% endfor %}
                {% endif %}
            </ul>
        </li>
    {% endif %}
    {% endif %}
    {% endfor %}
    {% else %}
    {% if discounts|length > 0 %}
    <li class="offers-wrapper">
        <span class="offers-header open">
          <span class="title-line">
              <span class="flex">OFFRES ({{ discounts|length }})</span>
              <span class="fa fa-chevron-right"></span>
              <span class="fa fa-chevron-up"></span>
          </span>
          <div class="offers-bottom"></div>
        </span>
        <ul class="offers">
        {% if (iframe is defined and iframe) %}
        {% for discount in discounts %}
        {% if discount.isOnline == 1 and  discount.isMultisite == true %}
            <li>{{ discount.description }}</li>
        {% endif %}
        {% endfor %}
        {% else %}
        {% for discount in discounts %}
        {% if discount.isOnline == 1 %}
            <li>{{ discount.description }}</li>
        {% endif %}
        {% endfor %}
        {% endif %}
        </ul>
    </li>
    {% endif %}
    {% endif %}
</ul>

